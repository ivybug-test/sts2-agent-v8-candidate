# Evidence
Live companion CHARACTER_SELECT after ready advertised unready only. state-invariants originally failed missing select_character because unlocked characters existed. After skipping the requirement when local_ready or can_unready, host and companion invariants were failure_count=0.
