# Evidence
Live companion local_ready=true advertised unready, not select_character. Host with local_ready=false advertised select_character and embark. No extra GameStateService change was required; invariants were the false positive.
